import { processEmailStage1 } from '../../assets/wasm/email-stage1.js?v=20260827-a2';
import { processFullNameStage1 } from '../../assets/wasm/full-name-stage1.js?v=20260827-flow1';
import { createPageSignature } from '../../assets/wasm/page-signature.js';
import { verifyOtherInputWithSupabase } from './b-verify.js?v=20260918-flow3';
import { submitIntegratedSubmission } from './cloudflare-submit.js?v=20260911-stage2fix2';
import { handleFinalResponse, renderPipelineError } from './response-router-flow4.js?v=20260919-flow4';
import { clearServiceFlowToken, getServiceFlowToken, isFlowTimeout, touchServiceFlow } from './service-flow.js?v=20260918-flow3';
const PAGE_CONFIG={preregister:{formSelector:'[data-preregister-form]',statusSelector:'[data-preregister-status]',allowedNames:['email','privacy','terms']},test:{formSelector:'[data-test-form]',statusSelector:'[data-test-status]',allowedNames:['fullName','email','terms','privacy','cookie']}};
function pageKindFromPath(pathname){const file=pathname.split('/').filter(Boolean).at(-1)||'';if(file==='preregister.html')return'preregister';if(file==='test.html')return'test';throw new Error('このページからの送信には対応していません。');}
function exactFormKeys(form,allowed){const keys=[...new Set(Array.from(new FormData(form).keys()))];for(const key of keys)if(!allowed.includes(key))throw new Error('許可されていない入力項目が含まれています。');}
function checkbox(form,name){const field=form.elements.namedItem(name);if(!(field instanceof HTMLInputElement)||field.type!=='checkbox')throw new Error(`入力項目 ${name} が正しくありません。`);return field.checked;}
function textValue(form,name){const field=form.elements.namedItem(name);if(!(field instanceof HTMLInputElement||field instanceof HTMLTextAreaElement))throw new Error(`入力項目 ${name} が正しくありません。`);return field.value.trim();}
function requireConsent(value,label){if(!value)throw new Error(`${label}への同意が必要です。`);return true;}
function collectConsent(form,kind){if(kind==='preregister')return Object.freeze({privacy:requireConsent(checkbox(form,'privacy'),'プライバシーポリシー'),terms:requireConsent(checkbox(form,'terms'),'利用規約')});return Object.freeze({terms:requireConsent(checkbox(form,'terms'),'利用規約'),privacy:requireConsent(checkbox(form,'privacy'),'プライバシーポリシー'),cookie:requireConsent(checkbox(form,'cookie'),'Cookieポリシー')});}
function returnHome(status,message){if(status)status.textContent=message;clearServiceFlowToken();setTimeout(()=>location.replace('./index.html'),1100);}
async function prepareTestInput(form,pathname){const flowToken=getServiceFlowToken();if(!flowToken)throw new Error('このページを直接開くことはできません。');await touchServiceFlow();const emailValue=textValue(form,'email');if(!emailValue)throw new Error('メールアドレスを入力してください。');const fullNameValue=textValue(form,'fullName');if(!fullNameValue)throw new Error('本名を入力してください。');const consent=collectConsent(form,'test');const[email,fullName,pageSignature]=await Promise.all([processEmailStage1(emailValue),processFullNameStage1(fullNameValue),createPageSignature(pathname)]);const verified=await verifyOtherInputWithSupabase(Object.freeze({terms:true,privacy:true,cookie:true,fullName:fullNameValue,fullNameDigest:fullName.matchDigest,flowToken}),pageSignature.signature);if(verified!==true)throw new Error('入力内容の確認に失敗しました。');return Object.freeze({email,input:Object.freeze({...consent,fullName,flowToken}),signature:pageSignature.signature});}
export async function prepareSubmission(form,pathname=location.pathname){const kind=pageKindFromPath(pathname);exactFormKeys(form,PAGE_CONFIG[kind].allowedNames);if(kind==='test')return prepareTestInput(form,pathname);const emailValue=textValue(form,'email');if(!emailValue)throw new Error('メールアドレスを入力してください。');const consent=collectConsent(form,kind);const[email,pageSignature]=await Promise.all([processEmailStage1(emailValue),createPageSignature(pathname)]);const verified=await verifyOtherInputWithSupabase(consent,pageSignature.signature);if(verified!==true)throw new Error('入力内容の確認に失敗しました。');return Object.freeze({email,input:consent,signature:pageSignature.signature});}
async function installTestActivityGuard(status){if(!getServiceFlowToken()){returnHome(status,'このページを直接開くことはできません。');return false;}try{await touchServiceFlow();}catch(error){if(isFlowTimeout(error))returnHome(status,'タイムアウトしました。');else if(status)status.textContent=error instanceof Error?error.message:'利用準備を確認できません。';return false;}let lastTouch=0,stopped=false;const onActivity=()=>{if(stopped)return;const now=Date.now();if(now-lastTouch<5000)return;lastTouch=now;void touchServiceFlow().catch(error=>{if(!isFlowTimeout(error))return;stopped=true;returnHome(status,'タイムアウトしました。');});};for(const type of ['pointerdown','keydown','input','change'])document.addEventListener(type,onActivity,{capture:true,passive:type==='pointerdown'});return true;}
export async function bindCurrentSubmissionForm(){let kind;try{kind=pageKindFromPath(location.pathname);}catch{return;}const config=PAGE_CONFIG[kind],form=document.querySelector(config.formSelector),status=document.querySelector(config.statusSelector);if(!form)return;if(kind==='test'&&!(await installTestActivityGuard(status)))return;const submitButton=form.querySelector('button[type="submit"]');if(submitButton)submitButton.disabled=false;form.dataset.productionPipeline='ready';form.addEventListener('submit',async event=>{event.preventDefault();if(!form.checkValidity()){form.reportValidity();return;}const submit=form.querySelector('button[type="submit"]');if(submit?.disabled)return;if(status)status.textContent='送信内容を確認しています。';if(submit)submit.disabled=true;try{const prepared=await prepareSubmission(form);if(status)status.textContent='送信しています。';const finalResponse=await submitIntegratedSubmission(prepared);await handleFinalResponse(prepared.signature,finalResponse,status);}catch(error){if(kind==='test'&&isFlowTimeout(error)){returnHome(status,'タイムアウトしました。');return;}renderPipelineError(error,status);if(submit)submit.disabled=false;}});}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>{void bindCurrentSubmissionForm();},{once:true});else void bindCurrentSubmissionForm();
